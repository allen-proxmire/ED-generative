# ED Gravity — Keystone Status: What Is Open, Precisely

**Allen Proxmire · June 2026 · a short, linkable status note for the ED gravity program**

This one-page note states, without hedging, exactly what remains open in the Event-Density (ED) emergent-gravity program and what would settle it. It is meant to be linked from the longer papers (the PhilPapers synthesis, GR-I/II/III, KM-I/II) so a reader can see the live edge at a glance. Every line is tiered.

---

## The one-line state

**Everything structural in the gravity line is in place; the preferred-frame number `α₁, α₂` — long the one open falsification front — now resolves *favorably* on ED's own primitives: commitment is sparse (forced by ED's quantum sector), so the khronon coupling is density-suppressed and `α₁` is safe by ≥70 orders. ED's gravity is a *screened* khronometric theory: observationally General Relativity, distinct only at the Planck-density frontier.**

## What is settled (derived or measured)

- **Emergent Lorentzian metric** from the bandwidth field `g ∼ b⁻¹`, lapse `N² ∼ b`. *(derived.)*
- **Weak-field Einstein metric**, Schwarzschild relation `g₀₀ g_rr = −1`, and **factor-of-two light bending** (ray-tracing ratio 2.09 vs 2.000). *(derived + measured.)*
- **Einstein, not Nordström** — the branch is forced by the irreversibility of commitment. *(derived.)*
- **Gravitational class is khronometric** — Einstein's two tensor modes plus one physical scalar (the khronon), by Lovelock + mode count. *(structural.)*
- **Both gravitational cones at light speed:** `c_T = c` (single transport cone) and `c_s = c` (the khronon, from `ε = 0`, because the relevant sector is dissipative not kinetic). *(derived.)*
- **Dynamical rule** `ḃ = D∇²b − κρ`: steady state is the Newtonian field equation (corr. 0.999), `r_s ∝ M`, and a **spontaneously forming horizon** carrying **area-law entropy `S ∝ A`** and **Hawking scaling `κ ∝ 1/r_h`**. *(measured / derived.)*
- **Matter follows the timelike geodesics** of the emergent metric (massive eikonal, correct relativistic velocity dependence). *(measured.)*
- **Newton's `G` is derived downstream** (`G = c³ℓ_P²/ℏ`): form forced, value inherited through the single Planck scale — one fewer independent constant. *(derived form, inherited value.)*

## The one open number

**`α₁, α₂` — the preferred-frame PPN parameters.** Bounds: `|α₁| ≲ 10⁻⁴`, `|α₂| ≲ 10⁻⁷`.

Status, after the direct (Route-B) analysis and the vector-sector build (June 2026):

- **Verified khronometric formulas** give `α₁ ∝ (α−2β)`, so `α₁ = α₂ = 0 ⟺ α = 2β`; ED's `c_T=c, c_s=c` reduce the problem to the **directed-flux (vector) sector** of the rule, which was then built and simulated.
- **The covariance escape is closed.** The boost-covariant `g_{0i}` of a moving mass is `4 w_i U`, so `α₁ = 0` would require ED to be boost-covariant. It is **not**: the rule is first-order in cosmic time, and a moving source produces a dipolar `O(w)` wake (sim-confirmed) that cannot be coordinate-removed because the cosmic frame is physical. So `α₁` is **not protected to zero by symmetry** — and it is the arrow (P11, the preferred frame) that forecloses the protection. ED must rely on a suppression mechanism, not a symmetry.
- **The EFT coarse-graining gives `α₁ = −4 c₁₄ + (dissipative O(w) correction)`**, where `c₁₄` is the *conservative* acceleration/foliation-bending coupling (tied to P13's homogeneous-tick stiffness; generically `O(1)`, not symmetry-forced to zero). The dissipative reserve is **damping** (`γ∂_t`, per the `ε=0` result), which cleans up the *radiative* khronon (vacuum gravitational waves) but does **not** statically screen the quasi-static `α₁`. An earlier "screening" estimate that treated the reserve as a static mass term — and so reported `α₁ ≈ 0` — over-extended the mechanism; that lean has been withdrawn.
- **Protection hunt — the result is firm and cuts both ways.** *(i) Exact protection is refuted:* `α₁ = 0` outright would need `c₁₄ = 0`, but that is incompatible with ED's established propagating khronon at `c_s = c` (with `β = 0`, `c_s = c` gives `c₁₄ = λ/(1+2λ)`, so `c₁₄ = 0 ⟹ λ = 0 ⟹` GR, no khronon). So `α₁ ≈ −4λ` is genuinely **nonzero**, locked to the khronon expansion coupling `λ = c₂`. *(ii) But natural-smallness is established:* small `c₁₄` is **technically natural** — the `c_i → 0` limit is GR (enhanced diffeomorphism symmetry), and ED's metric-only matter coupling (KM-I) shields it from radiative spoiling — so ED being PPN-safe is **not fine-tuning**.
- **Coarse-graining `λ` — leans unfavorable (the standard LV-gravity naturalness challenge).** `λ = c₂` is the khronon's kinetic weight — the (pseudo-)Goldstone decay constant of broken time-reparametrization, `λ ∼ f²/M_P²`. ED breaks time-diff at the **substrate (Planck) scale** (commitment/tick are fundamental), so the generic expectation is `f ∼ M_P ⟹ λ = O(1) ⟹ α₁ = O(1)`, in tension with the bound. Technical naturalness gives *stability* of a small `λ`, not its *generation*; Planck-scale breaking generically generates `O(1)`. This is the well-known unresolved problem of Lorentz-violating gravity — shared with Hořava/aether/ghost-condensate, **not unique to ED**, with viable members existing in a small-coupling corner ED is not shown to occupy.
- **Suppression hunt (`f ≪ M_P`) — no robust mechanism found; the lean hardens.** The standard rescues are all unavailable: no SUSY; the arrow's breaking is *fundamental* (no lower scale to use); the khronometric couplings are *marginal* (no emergent-Lorentz-invariance flow to zero); no custodial symmetry. The one ED-specific candidate — vacuum commitment *sparsity* — is genuine but **disfavored** (it runs against ED's ongoing-becoming ontology, conflicts with the dense near-matter commitment sector where `α₁` is measured, and needs an unexplained hierarchy in the dimensionless `λ`).
- **Reopening (the sparsity mechanism, on ED's terms).** The `α₁ = O(1)` verdict rested on importing the field-theory default (rigid *explicit* Lorentz violation) — but the **arrow gives time-asymmetry (T-violation), which is *not* a preferred frame (Lorentz-violation)** (the Standard Model is T-violating yet exactly Lorentz-invariant). And **V1 finite-speed causation makes spacelike commitments causally independent**, so ED's preferred frame is largely the *spontaneous cosmological* (Hubble/CMB-like) frame — Lorentz-invariant, `α₁ = 0` — with only a **soft explicit residue**. The suppression-hunt's three objections all dissolve (ongoing T-violation ≠ dense foliation-pinning; gravity is the rate-independent `κ/D` ratio, not the commitment density; the stiffness hierarchy is supplied by spontaneous-vs-substrate). So `λ ≪ 1` is natural, not forced large — the `α₁ = O(1)` verdict is **withdrawn as forced.**
- **The mechanism, made concrete: intrinsic density-dependent screening.** Because gravity *is* event density and that density is **non-uniform** (sparse vacuum, dense matter), the arrow breaks time-reparametrization *where events commit* — so the breaking scale is the **local event density**, `f(x) ∼ √ρ_event`, **not** `M_P`. This gives a density-dependent coupling `λ(ρ) ∼ ρ_event/ρ_Planck` — an intrinsic **chameleon/symmetron-type screening** (the standard, accepted way modified gravity evades Solar-System tests while staying cosmologically active). In the low-density orbital vacuum where `α₁` is tested, `λ ≪ 1` by *many orders* (any sub-Planck density), so `α₁` is suppressed **by structure**, while gravity (the rate-independent `κ/D` ratio) and cosmology are untouched. The `α₁ = O(1)` verdict was an artifact of a uniform-background assumption ED does not have — withdrawn.
- **`λ(ρ)` derived from the primitives.** The two stiffnesses come from *different* primitives: the metric stiffness `M_P²` is the **always-on P02 sharing** (density-independent), while the khronon stiffness `f²` is the **sparse P11 commitment-pinning** (density-dependent). Their ratio is the local commitment sparsity: **`λ = (k₁₁/s₀₂)·ρ_event/ρ_Planck ∼ ρ_event/ρ_Planck`** (linear in vacuum, saturating to `O(1)` near matter). So the breaking scale is `f² ∼ ρ_event ℓ_P²` (the *local* event-density scale), **not** `M_P` — the field-theory default's assumption, replaced by ED's own structure. This gives intrinsic chameleon/symmetron screening, and an elegant consequence: **as `λ → 0` in vacuum, all khronometric couplings vanish — ED is literally General Relativity in the deep-vacuum limit (`α₁ = 0`, Lorentz-invariant) and khronometric only near matter.** Checked consistent with `c_T = c`, vacuum `c_s = c` (the `ε=0` locking gives `c_s → c` as `λ → 0`; GW170817 is the vacuum regime), and `κ/D = 8πG` (the density-independent gravity ratio).
- **Magnitude computed — safe by ≥70 orders on the sparse branch.** The whole number reduces to one sharp ED question: **is commitment (P11) a *sparse physical determination event* or a *dense every-tick* process?** P11 (commitment = irreversible determination) and P13 (the tick = clock) are *separate* primitives, so a coherent region does not commit merely because the clock ticks — commitment is **sparse**, `ρ_event ≪ ρ_Planck`. On that branch (which ED's ontology favors), every plausible identification of the Solar-System event density gives `ρ_event/ρ_Planck` between `∼10⁻⁹³` (source) and `∼10⁻¹²²` (cosmological), so **`α₁ = −4λ ≲ 4×10⁻⁹³`, below the bound `2.5×10⁻⁵` by ≥ 88 orders of magnitude** (≥70 even at an absurd nuclear-density stress-test). The margin is so vast the identification uncertainty is irrelevant to safety. (Dense branch — every tick commits — gives `α₁ = O(1)`, tension; it is the disfavored field-theory reading that makes P11 redundant with P13.)
- **The sparse branch is forced — the conditional becomes a verdict.** Commitment (P11) is a sparse, local, *decoherence-like* determination event, distinct from the tick (P13). The dense branch (`P11 ≡ P13`) is **excluded decisively**: if every tick committed every degree of freedom, no superposition could persist — *no quantum mechanics* — and ED has a quantum sector. So commitment is sparse on pain of destroying ED's own QM. (Two further forcings agree: P11 needs persistent indeterminacy to act on; gravity must track matter via the `−κρ` sink, not the clock.) Hence `ρ_event ≪ ρ_Planck`, `λ ≪ 1`, and **`α₁ ≲ 4×10⁻⁹³` — safe by ≥70 orders, not conditionally but by ED's primitives.**
- **Standing: the `α₁` front is closed favorably.** ED is *observationally General Relativity everywhere gravity is tested* and khronometric only at the Planck-density frontier — its Lorentz violation density-suppressed, derived (not assumed), traced to the very coherence that gives ED quantum mechanics. **The deep result — sparse commitment is *constitutive*, not a chosen branch.** Dense commitment (collapse every tick) is the **quantum Zeno limit**: continuous measurement *freezes* all evolution — no superposition, interference, coherence, or unitarity, no dynamics at all. ED has a quantum sector, so it *must* be sparse. That same structure — unitary becoming punctuated by rare irreversible commitments — has three faces: **quantum mechanics** (sparse collapse avoids the Zeno freeze), **gravitational time dilation** (becoming at a position-dependent proper-time rate = the lapse; "you can't have time dilation if it ticks the same"), and **`α₁`-safety** (sparse `⟹ λ_local ≪ 1`). Preferred-frame safety is a *corollary* of ED being a quantum, time-dilating theory at all — which is why the front closed cleanly: the sparse branch was never optional. Consistent with `c_T = c`, vacuum `c_s = c` (GW170817), `κ/D = 8πG`. **No value fabricated.**
- **MOND reconciliation — done (no conflict, a distinction).** MOND's `a₀ = cH₀/(2π)` is derived (Paper_029) from the **cosmic decoupling surface** `R_H = c/H₀` projected onto an accelerating chain — a cosmic-boundary kinematic effect set by `H₀`, with *no* local khronon coupling in it. So MOND is the khronon's *cosmological* face, while `α₁ = −4λ_local` is its *local* face (`λ_local ∼ ρ_event/ρ_Planck ≪ 1`). Different quantities, different scales — suppressing `λ_local` leaves `a₀` untouched. ED has MOND *and* `α₁`-safety simultaneously. (And MOND is strongest in *low*-density outskirts, exactly where `λ_local` is *smallest* — so MOND could never have been a `λ_local` effect.) The remaining residue is only the order-of-magnitude nature of the `α₁` value (safety robust by ≥70 orders regardless). After this 19-step arc: ED's gravity is a screened khronometric theory — observationally GR locally, MOND-phenomenological galactically (via the cosmic boundary), `α₁`-safe, falsifiably distinct only at the Planck-density frontier — all from one foliation field at two vastly separated scales.

## Other honest debts (named, not gravity-proper, or hardening)

- **Cosmological-constant magnitude** — sign, structure, and `Λ ∼ H₀²` scaling derived; the magnitude is the cosmological-constant smallness problem, **open**.
- **Deep-MOND small-scale origin** — reproduced phenomenologically; the first-principles derivation is deliberately **unattempted (guarded)**.
- **Clusters + CMB**, and **quantum-information unitarity** (Page-curve *structure* reproduced; full unitarity open).
- **Strong-field / nonlinear solutions**; a fully-direct near-horizon-slope simulation (resolution-limited); a clean 3D `1/r` confirmation. **Hardening, not blocking.**

## The honest standing

The theory could have lost, and the place it could lose was marked: `α₁, α₂`. That front is now **closed favorably** — and not by a structural lean alone (a lean is not a number), but on ED's own primitives: commitment is *sparse* (forced — dense commitment is the quantum Zeno limit, and ED has a quantum sector), so the local khronon coupling is density-suppressed and `α₁ = −4λ_local ≲ 4×10⁻⁹³`, below the bound by ≥70 orders. The Route-A/Route-B distinction now sets the *residual*, not the verdict: the direct PPN expansion (Route B) would tighten the order-of-magnitude of a number whose safety is already robust by ≥70 orders — a hardening, not a jeopardy. The live debts have moved off the falsification front onto the named items above (cosmological-constant magnitude, clusters/CMB, full unitarity, strong-field). That is the program's edge, stated plainly: the sharp front held.

---

*Companion to the ED gravity papers. The full derivations live in the foundations notes (`Phase3_GR_*`, `KhrononMOND_*`) and the published papers GR-I/II/III and KM-I/II; this note is only the live open/closed edge, kept short on purpose.*
