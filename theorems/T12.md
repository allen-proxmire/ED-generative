# T12 — U2-Continuum (Inner-Product Extension to Continuous Configurations)

**Status:** ~~FORCED-unconditional (ratified 2026-04-26)~~ **DOWNGRADED 2026-07-10 — see correction below.**
**Layer:** Theorem-level structural result.

## CORRECTION 2026-07-10

Re-audited during the T14 cascade re-audit (the follow-on flagged by `T14.md`'s 2026-07-06 downgrade). Two findings, one exonerating and one not:

1. **NOT a T14 dependency (exonerated on that count).** T12 uses complex wavefunctions `Ψ: M → ℂ` but contains no ℝ/ℍ-exclusion argument and never invokes "ℂ is uniquely forced." The complex carrier enters only as an already-granted object (`I-U1` + `Paper_007`'s P-Motif-Algebra postulate), so T12 does **not** inherit T14's specific ℂ-uniqueness defect.
2. **But it INHERITS T11's downgrade (the real problem).** T12 does not re-derive the inner product for the continuum — it is a straight DCGT coarse-graining of **T11**'s discrete inner product, which is itself downgraded (see [T11](T11.md)). The derivation *starts* from `⟨P,Q⟩ = Σ_K P_K* Q_K` (Paper_004_6 §3.1, audit step 4 labeled `I` = inherited from Paper_U2_Discrete), and that bare diagonal sum with no cross-terms **is** the channel-orthogonality structure `⟨K|L⟩ = 0` that T11 never established (the open **P-Channel-Orthogonality** conjecture). DCGT maps `Σ_K → ∫ dμ` carrying the same unestablished orthogonality into the continuum, and §3.5 explicitly re-exports T11's now-open Bell–Tsirelson `|S| ≤ 2√2`. No independent continuum re-derivation of the orthogonality foundation exists in the paper.

**Current honest status:** **FORM-CONSISTENT conditional on P-ScaleSeparation (the DCGT regime) AND on the open P-Channel-Orthogonality conjecture** (via T11) — not FORCED-unconditional. Independent of T14's ℂ-uniqueness claim. The one new assumption T12 adds (`P-ScaleSeparation`) governs whether the extension is valid but does nothing to close the orthogonality gap it inherits.

## Summary (superseded, preserved for provenance)

States that the discrete-channel inner-product structure of T11 extends to continuous-configuration Hilbert spaces FORCED-uniquely under the description-level continuum gauge convention. Together with T11, this completes the Hilbert-space arena for ED's Phase-1 program. Originated in Arc U2_continuum.

## Reference

The full statement, derivation, and discussion of this theorem live in this repository under `arcs/U2_continuum/` and the corresponding paper at `papers/U2_Inner_Product/`. The constitutional registration of this theorem is recorded in:

- `ED-primitives/foundations/forced_theorems_inventory.md` — name-only registry
- `ED-primitives/CONSTITUTION.md` — governance

This file in `event-density/theorems/` is the theory-side index entry, not the proof.
